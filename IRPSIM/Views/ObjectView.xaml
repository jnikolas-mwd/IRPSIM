<UserControl x:Class="IRPSIM.Views.ObjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:mvvm="clr-namespace:IRPSIM.MVVM"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodel="clr-namespace:IRPSIM.ViewModels"
              mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ContentControl Grid.Row="0" x:Name="SelectedObjectHelper" Content="{Binding SelectedObject, Mode=OneWayToSource}" Visibility="Collapsed"/>
        <TreeView Grid.Row="1" SelectedItemChanged="TreeView_SelectedItemChanged">
            <TreeViewItem Header="Supply" ItemsSource="{Binding SupplyVariables.View}"/>
            <TreeViewItem Header="Demand" ItemsSource="{Binding DemandVariables.View}"/>
            <TreeViewItem Header="Storage" ItemsSource="{Binding StorageVariables.View}"/>
            <TreeViewItem Header="Cost" ItemsSource="{Binding CostVariables.View}"/>
            <TreeViewItem Header="Scenarios" ItemsSource="{Binding Scenarios.View}"/>
            <TreeViewItem Header="Scripts" ItemsSource="{Binding Scripts.View}"/>
            <TreeViewItem Header="Categories" ItemsSource="{Binding Categories.View}"/>
            <TreeViewItem Header="Definitions" ItemsSource="{Binding Definitions.View}"/>
            <TreeViewItem Header="Variables" ItemsSource="{Binding SupportingVariables.View}"/>
        </TreeView>
    </Grid>
</UserControl>
